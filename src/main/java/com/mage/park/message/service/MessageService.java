package com.mage.park.message.service;

import org.apache.log4j.Logger;
import org.springframework.stereotype.Service;

@Service
public class MessageService {
	
	private Logger logger = Logger.getLogger(this.getClass());
	
}
